// google-charts.d.ts

declare global {
  interface Window {
    google: any; // Declare 'google' as part of the window object with type 'any'
  }
}

export {}; // Ensure the file is treated as a module
// google-charts.d.ts

declare global {
  interface Window {
    google: any; // Declare 'google' as part of the window object with type 'any'
  }
}

export {}; // Ensure the file is treated as a module
